import { sounds } from '../../data/sounds';
const app = getApp();
Page({ data: { state: {}, sound: {}, recent: [] }, onLoad() { this.unsubscribe = app.player.subscribe(() => this.render()); this.render(); }, onShow() { this.render(); }, onUnload() { this.unsubscribe?.(); }, render() { const state = app.player.getState(); this.setData({ state, sound: sounds.find((item) => item.id === state.soundId), recent: app.storage.getRecentSoundIds().map((id) => sounds.find((item) => item.id === id)).filter(Boolean) }); }, toggle() { app.player.toggle(); }, previous() { this.move(-1); }, next() { this.move(1); }, move(delta) { const index = sounds.findIndex((item) => item.id === app.player.getState().soundId); app.player.select(sounds[(index + delta + sounds.length) % sounds.length].id); }, mute() { app.player.setMuted(!app.player.getState().muted); }, select(event) { const id = event.currentTarget.dataset.id; app.player.select(id); app.player.play(); app.storage.recordRecent(id); this.render(); } });
